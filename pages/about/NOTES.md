# /pages/about

Static About page content (story, values). Backs [/about.html](../../about.html).

## Files

- **about.css** — two-column story layout, values grid.
- No JS file — this page is fully static (header/footer are still rendered via `/shared/header.js` and `/shared/footer.js`). Add `about.js` here if the page later needs dynamic content (e.g. team members from JSON).

## Data source

None — all content is hardcoded in about.html since it's static prose, not catalog data.
